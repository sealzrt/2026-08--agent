# 2026-08-19

## ai-agent学习/06.LangGraph入门 教程优化

- 用户反馈 06.LangGraph入门 目录下 15 篇 md 正文（每篇 3-4KB）过于简单、内容单薄。
- 已完成现状诊断：正文骨架化（9 段齐全但每段一两句）、零图示、无运行证据、与 code/step_by_step、code/langgraph_intro_project、langgraph/(60+ notebook) 完全脱节、main.py 是 PyCharm 残留、根 README 仅一行。
- 用户指定内容标杆：`LangGraph-笔记/`（单篇 55-370KB，有原理深挖/对比表/截图/版本标注/FAQ）。
- 已输出方案文档：`06.LangGraph入门/LangGraph入门优化方案.md`。
  - 12 段统一结构（新增导读、执行过程拆解、调试与排查、本章速查）
  - 每章 10-15KB、≥1 张 mermaid 图、≥1 张对比表
  - 借鉴 LangGraph-笔记 手法但保留 06 差异化（贯穿项目、渐进式、三索引）
  - 执行顺序：样板批(03/04) → 核心批(01-09) → 应用批(10-12) → 收尾批(13-15+README)
- 下一步待用户确认：从样板批 03、04 两章开始增强。

## 样板批完成（03、04 两章）

- 已按 12 段结构重写 03、04 两章：03=13.5KB、04=17.4KB（目标 10-15KB）。
- 新增内容：本章导读、执行过程拆解（状态快照表）、调试与排查（报错样例+排查）、本章速查（API 表+概念定义+加深阅读索引）、mermaid 图、对比表。
- 已运行验证：03_first_graph.py、04_state_graph.py 输出与文档一致。
- 重要发现并已写入文档：
  1. `print_ascii()` 需要 `grandalf` 库（`pip install grandalf`），未装报 ImportError；零依赖方案是 `graph.get_graph().draw_mermaid()`。
  2. LangGraph 不自动补 TypedDict 字段默认值：节点读取缺失字段才 KeyError；无节点读取则最终状态静默缺字段。
  3. list 默认覆盖行为已实测：后写覆盖先写；`Annotated[list[str], add]` 追加正确。
- 用户确认风格后继续：核心批(01-09)、应用批(10-12)、收尾批(13-15+README)。

## 核心批完成（01、02、05-09 共 7 章）

- 全部按 12 段结构重写完成，篇幅 13-18KB（01=14.5K、02=14.8K、05=13.4K、06=13.8K、07=13K、08=14.2K、09=14.2K）。
- 每章含 mermaid 图 + 对比表 + 完整预期输出 + 调试排查 + 本章速查（含 notebook/笔记索引）。
- 批量回归验证 02-09 全部 OK（08_llm_real 需 API Key 未跑）。
- 验证中发现并修正的关键点：
  1. 06 章"LangGraph 条件边"len=12>8 走检索分支（原文档误判为走 summarize），已重写拆解并实测输出"基于 1 份资料"。
  2. 09 章达标分数 90 不改变轮次（2 份资料即得 90 分），改用 95 演示轮次变化（实测 iteration_count=3）。
  3. GraphRecursionError 真实报错已实测："Recursion limit of 10007 reached without hitting a stop condition..."，已写入文档。
- 剩余：应用批(10-12)、收尾批(13-15+README 重写+映射表+清理 main.py)。

## 应用批完成（10、11、12 共 3 章）

- 全部按 12 段结构重写，篇幅 13-14KB（10=13.1K、11=13.8K、12=14K）。
- 代码验证全部 OK（10_persistence、11_human_in_the_loop、12_stream_output）。
- 重要实测与更新：
  1. 重写了 `10_persistence.py`：原示例第二次 invoke 手动传 notes（演示不到位），改为"同 thread 第二次不传 notes 从 checkpoint 恢复 + 新 thread 缺字段 KeyError"三种场景，实测确认。
  2. 实测确认：同 thread 恢复时未提供字段从 checkpoint 补上；新 thread 无历史则 KeyError（验证第 04 章"不自动补默认值"结论）。
  3. 11 章 interrupt/Command 时序、幂等副作用、多 interrupt 限制均写入，含 mermaid 时序图。
  4. 12 章 stream_mode 全表、stream vs stream_events 对比、SSE 事件结构示例、五字段统一事件结构。
- 剩余：收尾批(13-15+README 重写+四索引映射表+清理 main.py)。

## 收尾批完成（13、14、15 + README）——教程优化全部完成

- 13、14、15 章按 12 段结构重写：13=14K、14=15.1K、15=11.9K。
- 13 章含 log_node 包装器、测试分层（业务/节点/图）、mock 进 CI 策略。
- 14 章基于真实项目逐目录解释职责（config/state/nodes/tools/prompts/graph/api/tests），含分层总览表、python -m 导入陷阱。
- 15 章含部署架构 mermaid 图、三种运行方式、持久化迁移表、成本数字示例、16 项可勾选检查清单。
- README 重写为学习导航：四阶段路线 + 目录导航 + 环境要求 + 快速开始 + 15 行四索引映射表（章节↔step_by_step↔项目代码↔notebook↔LangGraph-笔记）。
- main.py（PyCharm 残留）已删除。
- 最终验证：15 章全部 11.9-17.9KB；step_by_step 全部可离线运行 OK（08_llm_real 需 API Key 属预期）；项目 pytest 11 passed；examples/run_graph.py 全链路（含人工驳回+意见）走通，质量评分 85。
- 教程优化整体完成：15 章从 3-4KB 骨架文档升级为 12-17KB 完整教程，含图/表/预期输出/调试/速查/三索引。

## 全量检查（用户要求"检查一遍"）

- 用脚本扫描 15 章 12 段结构：初查 8 个文件有缺失，已全部修复。
- 修复内容：
  - 01 章补"执行过程拆解"（拆解 run_research_agent 分支）+ mermaid 图。
  - 02、07、08、12、13 章各补 1 张 mermaid 图（02 状态流转、07 工具链路、08 mock/real 切换、12 事件流、13 测试分层金字塔）。
  - 14 章补"核心概念"段（单向依赖/薄节点/配置集中 + 分层 mermaid 图）。
  - 15 章补"最小代码示例"（部署配置）、"执行过程拆解"（任务时序 sequenceDiagram）、"贯穿项目改造"三段。
- 复查通过：15 章 12 段齐全、26 个 mermaid 块语法健康（括号/引号平衡）、路径引用全部有效、交叉引用章节正确、目录整洁（main.py 已删）。
- 最终回归：step_by_step 12 个示例全 OK、项目 pytest 11 passed。

## 二次审查改进（用户要求"再检查/补图/调序/加说明"）

- 补 8 张图（mermaid 26→34）：
  - 04 状态合并机制图（为什么只返回局部字段、其他字段还在）
  - 06 条件边"标签→path_map→目标"映射图
  - 09 循环轮次状态演进图（documents/iteration_count 逐轮增长）
  - 10 输入=增量更新图（有 checkpoint 恢复 vs 无 checkpoint KeyError 对比）
  - 12 节点状态机图（stateDiagram-v2 两张：普通节点 + human_review interrupted）
  - 08 JSON 解析兜底流程图（不可信输入→结构化输出的防御流水线）
  - 05 三层结构图（节点层→业务层→工具层，单向依赖）
  - 15（前面补过一次，本次 34 块总数含新增）
- 调整 1 处顺序：15 章"最小 API 回顾"从"最小代码示例"之前移到"贯穿项目改造"之后（API 属于产品形态，随最终形态一起讲）。
- 补 6 处难度说明：
  - 03 "为什么可以只返回局部更新"（类比只更新 user.name）
  - 04 `Annotated[X, Y]` 是什么（贴附加信息给 LangGraph 读，运行期仍是 list[str]）
  - 09 `recursion_limit` 在 config 顶层 vs `thread_id` 在 configurable 的区别
  - 10 为什么少传字段还能跑（invoke input=增量更新）
  - 11 interrupt 通过 GraphInterrupt 异常实现暂停（解释为何不能 try/except）
  - 06 path_map 拆"条件"与"映射"的声明式意义
- 复查：结构 12 段齐全、34 个 mermaid 块语法健康、15 章顺序验证通过。
