# 2026-08-25 工作日志

## ontology-demo / 需求文档图 1 优化（已完成）

- 目标：分析并优化 `ontology-demo/方案/01.需求文档/项目风险管控工具-需求规格说明书-v1.0.md` 第 1.1 节"产品定位"下的图 1
- 原图问题（6 项）：
  1. P1/P2/P3 编号与全文 P0/P1/P2 优先级冲突
  2. 与"风险管控"产品定位脱节（无风险语义）
  3. 节点粒度与第 3.1 节端到端流程不对应
  4. 纯线性、无闭环
  5. 无"风险"语义
  6. Mermaid 默认样式弱、阶段无区分色
- 优化设计（用户确认）：三阶段卡片分色（蓝/青/紫） + 顶部 amber 定位条"本体驱动：五类输入文档 → 要素抽取 → 规则推理 → 风险台账 · 决策链回放" + 每阶段底部红色"典型风险"标签 + 底部虚线回流闭环"质保缺陷 → 需求变更回流"
- 落地形式：仅 PNG 图片（用户选择）。SVG 源文件保留在 `01.需求文档/assets/fig1-risk-control-full-lifecycle.svg` 供后续编辑再导出
- 路径：
  - PNG 嵌入位置：`01.需求文档/项目风险管控工具-需求规格说明书-v1.0.md` 第 18 行（原 Mermaid 块替换为图片引用）
  - 资源目录：`01.需求文档/assets/`（新建，约定后续图也用此目录）

## 渲染工作流验证（新增能力）

- **透明背景 PNG 截图**：Chrome headless 加 `--default-background-color=00000000` 验证可生成透明背景 PNG，深色卡片 SVG 在白底/深底文档里都好看
- 复用并扩充了 `本体论-agent` 教程项目的 SVG→PNG 工作流（白底 HTML 包装页 + 本地 http.server + Chrome `--headless --force-device-scale-factor=2`）
- 适用于：内部 SVG 设计稿 → 透明 PNG 嵌入 Markdown 文档

## 待办

- 其余 7 张 Mermaid 图的体检报告已输出，等用户决定哪些需要进一步优化
- 图 1 优化稿的视觉细节（节点宽度 160、阶段容器 560 宽、左右各留 20px 通道）可作为后续图的参考样式
