# 第 2 章学习路径重排计划

**目标：** 在不删减现有技术内容的前提下，重排第 2 章的学习顺序，并用流程图、时序图和决策图降低理解成本。

**原则：**

- 先讲 Agent 一轮任务如何运行，再讲 `async function*` 的实现机制。
- 原有 API、消息类型、依赖注入和状态转换内容全部保留。
- API 名称作为源码阅读和检索线索，不作为需要背诵的知识。
- 每张图只表达一个核心问题，并紧跟“读图结论”。

## 目标顺序

```text
阅读地图
  -> 完整 Turn 全景
  -> Turn 生命周期
  -> State / Continue / Terminal 决策
  -> async function* 与流式事件
  -> 消息与事件类型
  -> 依赖注入与测试
  -> 练习与复习图
```

## 图解清单

- Agent 一轮任务总流程图。
- 模型、工具、消息和下一轮之间的时序图。
- `State / Continue / Terminal` 决策树。
- Continue 恢复路径图。
- 模型流、Harness、UI 三层数据流图。
- UI 展示路径与模型上下文路径对比图。
- 依赖注入测试结构图。

## 验证标准

- 现有 API、消息类型、终止原因、压缩策略和练习内容仍能在章节中找到。
- 章节开头可以在一张图中说明完整对话循环。
- 状态转换的决策逻辑有图形化表达，不只依赖表格和文字。
- `async function*` 语法说明放在完整运行流程之后。
- Markdown 代码围栏成对，`git diff --check` 无问题。
