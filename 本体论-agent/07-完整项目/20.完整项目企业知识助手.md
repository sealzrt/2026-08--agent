# 20. 完整项目：企业知识助手

## 本章目标

- 把本体、知识图谱、文档检索、工具、HITL 和前端/API 串成完整项目。
- 对比本地工具型 Agent 和服务 API 型 Agent。
- 设计项目目录、核心接口、数据表和端到端时序。
- 明确哪些内容属于教程最小版，哪些属于生产增强版。

## 1. 端到端总体架构

```mermaid
flowchart TB
    subgraph Client[交互层]
        Web[Web 前端<br/>普通用户和审核人]
        CLI[本地 CLI<br/>开发和演示]
    end
    subgraph API[业务服务层]
        TaskAPI[任务 API<br/>创建、补充、查询]
        ReviewAPI[审核 API<br/>批准、驳回]
        EventAPI[事件 API<br/>流式输出和历史]
    end
    subgraph Agent[Agent 编排层]
        Graph[LangGraph<br/>状态、路由、中断恢复]
        Ontology[本体服务<br/>类型、关系、约束]
        Planner[规划器<br/>意图和查询计划]
    end
    subgraph Capability[能力层]
        KG[知识图谱 / RDF Store]
        Docs[文档库和向量索引]
        Tools[业务工具 / API]
        Policy[权限和审核策略]
    end
    subgraph Store[存储层]
        DB[业务数据库<br/>task/run/event/audit]
        CP[Checkpointer<br/>thread_id 状态链]
    end
    Web --> TaskAPI
    Web --> ReviewAPI
    CLI --> TaskAPI
    TaskAPI --> Graph
    ReviewAPI --> Graph
    Graph --> Ontology
    Graph --> Planner
    Graph --> KG
    Graph --> Docs
    Graph --> Tools
    Graph --> Policy
    Graph <--> CP
    TaskAPI --> DB
    ReviewAPI --> DB
    EventAPI --> DB

    classDef client fill:#E0F2FE,stroke:#075985,color:#0F172A,stroke-width:2px;
    classDef api fill:#FEF3C7,stroke:#92400E,color:#0F172A,stroke-width:2px;
    classDef agent fill:#DCFCE7,stroke:#166534,color:#0F172A,stroke-width:2px;
    classDef cap fill:#FFFFFF,stroke:#111827,color:#0F172A,stroke-width:2px;
    classDef store fill:#FEE2E2,stroke:#991B1B,color:#0F172A,stroke-width:2px;
    class Web,CLI client;
    class TaskAPI,ReviewAPI,EventAPI api;
    class Graph,Ontology,Planner agent;
    class KG,Docs,Tools,Policy cap;
    class DB,CP store;
```

**图 20-1：企业知识助手端到端架构**

架构核心是职责分离：业务数据库保存任务和事件，checkpointer 保存图状态，知识图谱保存领域事实，文档库保存非结构化证据。

## 2. 本地工具型和服务 API 型对比

```mermaid
flowchart LR
    subgraph Local[本地工具型 Agent]
        L1[用户命令] --> L2[本地进程]
        L2 --> L3[本地文件 / 内存状态]
        L2 --> L4[终端输出]
    end
    subgraph Service[服务 API 型 Agent]
        S1[前端 / 第三方系统] --> S2[API 服务]
        S2 --> S3[Worker + LangGraph]
        S3 --> S4[持久化 checkpoint]
        S2 --> S5[任务、事件、审计表]
        S2 --> S6[SSE / WebSocket / 轮询]
    end

    classDef local fill:#E0F2FE,stroke:#075985,color:#0F172A,stroke-width:2px;
    classDef service fill:#DCFCE7,stroke:#166534,color:#0F172A,stroke-width:2px;
    class L1,L2,L3,L4 local;
    class S1,S2,S3,S4,S5,S6 service;
```

**图 20-2：本地工具型与服务 API 型**

本地工具型适合学习和个人自动化，状态管理较轻；服务 API 型需要用户隔离、任务表、事件流、审核入口和持久化 checkpoint。教程可以先本地跑通，再服务化。

## 3. 推荐目录结构

```text
enterprise_knowledge_agent/
├── app/
│   ├── api/              # task/review/event 接口
│   ├── graph.py          # LangGraph 编排
│   ├── state.py          # 图状态类型
│   ├── nodes/            # 本体、检索、工具、审核、回答节点
│   ├── ontology/         # 本体加载、实体链接、约束校验
│   ├── retrieval/        # 图检索、关键词、向量、证据合并
│   ├── tools/            # 业务工具封装
│   ├── policy/           # 权限、风险、审核策略
│   └── storage/          # task/run/event/audit/checkpoint 适配
├── data/
│   ├── ontology/
│   ├── instances/
│   ├── documents/
│   └── evaluation/
├── tests/
└── README.md
```

## 4. 端到端时序

```mermaid
sequenceDiagram
    participant U as 普通用户
    participant R as 审核人
    participant API as 业务 API
    participant W as Worker / LangGraph
    participant O as 本体服务
    participant KG as 知识图谱
    participant V as 文档和向量检索
    participant T as 业务工具
    participant DB as 任务事件审计库
    participant CP as Checkpointer

    U->>API: 创建任务：诊断产品 A 登录失败
    API->>DB: 写 task_id、thread_id、turn_id
    API->>W: invoke(input, thread_id)
    W->>CP: 保存和读取图状态
    W->>O: 实体、意图、关系校验
    W->>KG: 查询产品、模块、问题和解决方案
    W->>V: 检索文档证据
    W->>T: 执行 dry_run 诊断
    T-->>W: 返回诊断结果
    W->>DB: 写事件和证据
    W-->>API: 返回回答或中断
    API-->>U: 展示进度、证据和结果
    W->>API: 高风险动作需要审核
    API->>DB: 创建审核事件
    R->>API: 批准或驳回
    API->>W: Command(resume=feedback), 同一 thread_id
    W->>CP: 从断点恢复
    W-->>API: 返回最终状态
    API-->>U: 展示最终结果
```

**图 20-3：完整项目端到端时序**

这条时序把普通用户、审核人、业务 API、图执行、存储和外部能力分开。任何高风险动作都必须先形成审核材料，再由审核人反馈恢复执行。

## 5. 核心数据表

| 表 | 关键字段 | 用途 |
|---|---|---|
| `tasks` | `task_id`, `user_id`, `thread_id`, `status` | 完整业务任务 |
| `runs` | `run_id`, `task_id`, `turn_id`, `status`, `started_at` | 单次输入或恢复执行 |
| `events` | `event_id`, `task_id`, `run_id`, `event_type`, `payload` | 前端事件和执行日志 |
| `approvals` | `approval_id`, `task_id`, `interrupt_id`, `decision` | HITL 审核 |
| `audit_logs` | `actor_id`, `action`, `resource_id`, `policy_version` | 合规审计 |
| `evidence` | `evidence_id`, `task_id`, `source_id`, `content_hash` | 回答证据链 |

## 6. 最小 API

```text
POST   /tasks                         创建任务
POST   /tasks/{task_id}/messages       普通用户补充信息
GET    /tasks/{task_id}                查询任务状态
GET    /tasks/{task_id}/events         查询事件流
POST   /tasks/{task_id}/approvals      审核人提交批准或驳回
GET    /tasks/{task_id}/answer         查询最终回答和证据链
```

前端只需要使用 `task_id`。`thread_id` 属于后端内部恢复键，不应暴露为可由前端任意指定的参数。

## 7. 项目验收标准

1. 支持用户多轮补充同一个任务。
2. 能用同一个 `thread_id` 恢复 LangGraph 中断。
3. 能通过本体查询产品、模块、问题和解决方案。
4. 能合并图谱事实和文档证据。
5. 能在高风险动作时进入审核流程。
6. 能输出最终回答、证据链和不确定性说明。
7. 能保存任务、运行、事件、审核和审计记录。

## 本章小结

完整项目不是把所有能力塞进一个 Agent 函数，而是用业务 API、LangGraph、本体服务、检索、工具、权限和存储共同形成闭环。最小版先跑通端到端链路，生产版再增强持久化、并发、监控和权限。

