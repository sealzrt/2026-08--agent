# 11. Human-in-the-loop：人工确认与人工修改

## 本章目标

- 理解哪些 Agent 步骤需要人工介入。
- 使用 `interrupt()` 暂停图执行。
- 使用 `Command(resume=...)` 恢复图执行。
- 理解恢复执行会重新进入触发中断的节点。

## 为什么需要这一章

真实业务里，Agent 不能总是自动完成所有决策。比如最终报告发布前，需要人工确认；模型判断低置信度时，需要人工补充要求；涉及风险、审批、合规时，必须让人介入。

LangGraph 的 human-in-the-loop 能力让图在某个节点暂停，把中间状态交给人，然后根据人的反馈继续执行。

## 核心概念

### interrupt

`interrupt()` 用来暂停图执行，并向调用方返回需要人工处理的数据。

### Command(resume=...)

`Command(resume=...)` 用来把人工反馈传回图中，并恢复执行。

### 恢复执行语义

恢复执行时，会从触发 `interrupt()` 的节点开头重新运行。因此：

- `interrupt()` 前面的副作用必须幂等。
- 更稳妥的做法是把外部写入、发消息、扣费等副作用放到恢复之后。
- 不要把 `interrupt()` 包在 `try/except` 中捕获。
- 同一个节点里有多个 `interrupt()` 时，不要随意增删或重排。

## 最小代码示例

```python
from typing import TypedDict

from langgraph.checkpoint.memory import InMemorySaver
from langgraph.graph import END, START, StateGraph
from langgraph.types import Command, interrupt


class ResearchState(TypedDict):
    topic: str
    draft_report: str
    final_report: str


def write_draft(state: ResearchState) -> dict[str, str]:
    return {"draft_report": f"《{state['topic']}》的报告草稿"}


def human_review(state: ResearchState) -> dict[str, str]:
    feedback = interrupt({
        "draft_report": state["draft_report"],
        "question": "是否批准这份报告？",
    })

    if feedback.get("approved"):
        return {"final_report": state["draft_report"]}

    comment = feedback.get("comment", "需要修改")
    return {"final_report": f"{state['draft_report']}\n人工修改意见：{comment}"}


builder = StateGraph(ResearchState)
builder.add_node("write_draft", write_draft)
builder.add_node("human_review", human_review)
builder.add_edge(START, "write_draft")
builder.add_edge("write_draft", "human_review")
builder.add_edge("human_review", END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)


if __name__ == "__main__":
    config = {"configurable": {"thread_id": "review-001"}}

    first = graph.invoke(
        {"topic": "LangGraph HITL", "draft_report": "", "final_report": ""},
        config,
    )
    print(first)

    resumed = graph.invoke(
        Command(resume={"approved": False, "comment": "补充风险说明"}),
        config,
    )
    print(resumed)
```

## 贯穿项目改造

在最终报告前插入人工确认节点：

```text
generate_draft_report
  -> human_review
      -> approved: final_report
      -> rejected: revise_report
```

人工反馈 payload 示例：

```json
{
  "approved": false,
  "comment": "请补充资料来源和风险说明"
}
```

## 代码运行方式

```powershell
python 11_human_in_the_loop.py
```

## 常见错误

- 没有 checkpointer：中断后无法恢复。
- 没有传同一个 `thread_id`：恢复不到原来的会话。
- 在 `interrupt()` 之前执行不可重复副作用。
- 把人工反馈设计成复杂对象，无法序列化。

## 练习任务

必做：

- 增加 `approved_by` 字段，记录审批人。

扩展：

- 增加“驳回后重新总结”的分支，而不是直接生成最终报告。

## 本章小结

Human-in-the-loop 让 Agent 可以被人工接管。下一章会把图执行过程流式输出，方便前端展示进度和中断状态。

