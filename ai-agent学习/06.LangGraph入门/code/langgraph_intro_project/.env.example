# 运行模式：true=模拟（不调用真实模型），false=真实模式
USE_MOCK=true

# 流程控制
MAX_ITERATIONS=3
RECURSION_LIMIT=50
QUALITY_THRESHOLD=80

# 模型配置（真实模式使用，OpenAI 兼容接口）
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# LangSmith 观测（可选）
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=
