# 02. Python 最小基础

## 本章目标

- 创建一个最小 Python 项目。
- 理解本教程会用到的 Python 类型、函数和模块。
- 使用类型标注描述数据结构。
- 编写普通函数版的资料总结流程。

## 为什么需要这一章

你有服务端和前端经验，所以不需要从“变量是什么”开始学。但 LangGraph 示例大量使用 Python 的字典、类型标注、`TypedDict`、模块导入和虚拟环境。如果这些基础不稳，后面看 `StateGraph` 会很吃力。

本章只讲后续马上会用到的 Python。

## 核心概念

### 文件、模块和入口

一个 `.py` 文件就是一个模块。直接运行文件时，常见入口写法是：

```python
if __name__ == "__main__":
    main()
```

这类似服务端里的启动入口。

### 字典

LangGraph 的状态经常可以理解为一个字典：

```python
state = {
    "topic": "LangGraph 入门",
    "summary": "",
    "documents": [],
}
```

### 类型标注

类型标注不会自动限制运行时数据，但能提升可读性和编辑器提示：

```python
def summarize(topic: str) -> str:
    return f"主题总结：{topic}"
```

### TypedDict

`TypedDict` 用来描述字典应该有哪些字段：

```python
from typing import TypedDict


class ResearchState(TypedDict):
    topic: str
    summary: str
```

它非常适合入门阶段定义 LangGraph 的 `State`。

## 最小代码示例

创建文件 `02_basic_function.py`：

```python
from typing import TypedDict


class ResearchState(TypedDict):
    topic: str
    summary: str


def generate_summary(topic: str) -> str:
    return f"这是关于《{topic}》的入门总结。"


def run(state: ResearchState) -> ResearchState:
    summary = generate_summary(state["topic"])
    return {
        "topic": state["topic"],
        "summary": summary,
    }


def main() -> None:
    initial_state: ResearchState = {
        "topic": "LangGraph 适合做什么",
        "summary": "",
    }
    result = run(initial_state)
    print(result["summary"])


if __name__ == "__main__":
    main()
```

## 贯穿项目改造

这一章先不用 LangGraph，只建立贯穿项目的初始数据模型：

```python
class ResearchState(TypedDict):
    topic: str
    summary: str
```

后续会逐步增加：

- `questions`
- `documents`
- `quality_score`
- `iteration_count`
- `final_report`

## 代码运行方式

```powershell
python 02_basic_function.py
```

如果你还没有虚拟环境，可以先创建：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
```

## 常见错误

- `ModuleNotFoundError`：依赖没有安装，或没有激活虚拟环境。
- 中文乱码：确认编辑器和终端都使用 UTF-8。
- `KeyError`：读取了字典里不存在的字段，例如 `state["documents"]` 但没有初始化。

## 练习任务

必做：

- 给 `ResearchState` 增加 `documents: list[str]` 字段，并在 `run()` 中返回它。

扩展：

- 把 `topic` 改成从命令行输入，例如使用 `input()`。

## 本章小结

你已经有了一个普通 Python 函数版的资料总结流程。下一章会把这个函数包装成第一个 LangGraph。

