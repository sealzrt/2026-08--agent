# 07. 工具调用：让 Agent 连接外部能力

## 本章目标

- 理解工具函数和节点函数的区别。
- 编写一个可替换的模拟检索工具。
- 在节点中调用工具并写入 `State`。
- 设计工具失败时的返回结构。

## 为什么需要这一章

Agent 不能只靠模型“凭空回答”。真实业务里，它需要查数据库、读文件、请求 HTTP API、访问搜索服务或调用内部系统。这些外部能力可以封装成工具。

入门阶段先使用模拟检索工具，保证教程可复现。

## 核心概念

### 工具函数

工具函数负责访问外部能力。它不应该依赖 LangGraph。

```python
def search_tool(query: str) -> list[str]:
    return [f"模拟资料：{query}"]
```

### 节点函数

节点函数负责把状态转换成工具输入，再把工具输出写回状态。

```python
def search_documents(state: ResearchState) -> dict[str, list[str]]:
    documents = search_tool(state["topic"])
    return {"documents": documents}
```

这种拆分便于后续把模拟工具替换成真实工具。

## 最小代码示例

```python
from typing import TypedDict


class SearchResult(TypedDict):
    title: str
    content: str


class ResearchState(TypedDict):
    topic: str
    search_queries: list[str]
    documents: list[SearchResult]
    errors: list[str]


def mock_search_tool(query: str) -> list[SearchResult]:
    if not query.strip():
        raise ValueError("query 不能为空")

    return [
        {
            "title": f"{query} 入门资料",
            "content": f"这是一段关于 {query} 的模拟资料。",
        }
    ]


def search_documents(state: ResearchState) -> dict[str, object]:
    documents: list[SearchResult] = []
    errors: list[str] = []

    for query in state["search_queries"]:
        try:
            documents.extend(mock_search_tool(query))
        except Exception as exc:
            errors.append(f"检索失败：{query}，原因：{exc}")

    return {
        "documents": documents,
        "errors": errors,
    }
```

## 贯穿项目改造

第 07 章开始，建议把代码迁移到项目结构：

```text
code/langgraph_intro_project/
  app/
    state.py
    nodes/
      search.py
    tools/
      search_tool.py
```

拆分建议：

- `tools/search_tool.py`：只负责检索。
- `nodes/search.py`：只负责从状态取查询词、调用工具、返回状态更新。
- `state.py`：统一定义状态结构。

## 代码运行方式

如果仍使用单文件示例：

```powershell
python 07_tool_calling.py
```

如果使用项目结构：

```powershell
python -m app.nodes.search
```

## 常见错误

- 在工具函数里直接修改 LangGraph 状态。工具只返回结果，状态更新交给节点。
- 工具异常没有捕获，导致整个图直接失败。
- 工具返回结构不稳定，后续总结节点难以处理。

## 练习任务

必做：

- 给 `mock_search_tool()` 增加 `source` 字段。

扩展：

- 把模拟检索替换为本地 Markdown 文件检索。

## 本章小结

工具让 Agent 能连接外部世界。下一章会在节点中调用 LLM，并重点处理结构化输出。

