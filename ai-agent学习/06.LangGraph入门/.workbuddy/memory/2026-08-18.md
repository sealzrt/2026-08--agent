# 2026-08-18

## 完成：根据教程 md 生成配套 Python 代码

按 `06.LangGraph入门` 教程第 02~15 章 md 生成了可运行的代码：

- `code/step_by_step/`：02~13 单文件示例（02_basic_function.py、03_first_graph.py、04_state_graph.py、05_node_functions.py、06_conditional_edges.py、07_tool_calling.py、08_llm_mock.py、08_llm_real.py、09_loop_and_limit.py、10_persistence.py、11_human_in_the_loop.py、12_stream_output.py、12_api.py、13_debug_and_test.py）
- `code/langgraph_intro_project/`：14~15 完整项目（app 包含 config/state/graph/llm/nodes/tools/prompts/api，tests，examples，.env.example，pyproject.toml，README.md）

## 关键发现

- 本机 `python` 是 workbuddy 内置解释器（3.13.12），`pip` 指向系统 Python313，必须用 `python -m pip install`。
- 已装 langgraph 1.2.11：`graph.invoke` 遇到 interrupt 时**不抛 GraphInterrupt 异常**，而是返回带 `__interrupt__` 键的状态；恢复用 `Command(resume=...)`。
- md 中 13 章测试用的 summary 示例仅 22 字符，不足 mock 达标线 `len>40`，已把测试文本加长。

## 验证结果

- step_by_step 全部 13 个文件运行通过（含业务循环、HITL、流式输出）
- 项目 pytest 11 个测试通过
- 图模块、run_graph.py 交互、驳回反馈写入最终报告均验证通过
- fastapi/uvicorn 安装后 API 全流程验证通过：创建任务 -> interrupted -> 提交反馈 -> completed -> 获取报告

## 修复的问题

- langgraph 1.2.x 中 interrupt 不抛 GraphInterrupt 异常，项目代码改用检查 `__interrupt__` 键
- human_review 与 generate_final_report 职责冲突：human_review 只记录反馈，generate_final_report 依据反馈生成最终报告
- 教程 md 13 章测试 summary 示例不足 40 字符达标线，已加长测试文本
